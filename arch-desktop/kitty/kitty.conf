# Use the installed Inconsolata Nerd Font (ttf-inconsolata-nerd) so the Nerd Font
# glyphs in the starship prompt render instead of tofu boxes. The default kitty
# font_family (`monospace`) resolves to Noto Sans Mono, which has no icon glyphs.
# The "Mono" variant keeps every icon within a single terminal cell.
font_family Inconsolata Nerd Font Mono

include ./solarized_light.conf
